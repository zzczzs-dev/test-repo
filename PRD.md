# 今天吃什么 - 产品需求文档 (PRD)

## 一、产品概述

**产品名称**：今天吃什么

**产品类型**：Web 单页应用（H5）

**一句话描述**：帮助选择困难症用户快速决定今天吃什么的小工具

---

## 二、目标用户

| 用户群体 | 特征描述 |
|---------|---------|
| 选择困难症用户 | 每天面对"吃什么"问题时犹豫不决 |
| 上班族 | 时间紧张，希望快速做出餐饮决策 |
| 家庭用户 | 为全家做饭时需要多样化的菜品建议 |
| 饮食控制者 | 需要排除特定忌口食材的用户 |

---

## 三、核心痛点

### Pain Point 1：选择困难
- 每天重复面对"吃什么"问题
- 犹豫不决，浪费时间
- 往往最终随便吃点或重复吃同样的菜

### Pain Point 2：忌口难记
- 想排除某些食材但每次都要重新输入
- 忘记之前的忌口选择
- 外出就餐时难以表达饮食限制

### Pain Point 3：菜品单一
- 经常只想到常吃的几道菜
- 缺乏灵感，想尝试新菜品但不知道选什么
- 不了解菜品库中有哪些选择

---

## 四、核心功能

### 功能 1：随机推荐引擎
- 根据用户选择的分类（主食/荤菜/素菜/小吃）随机推荐菜品
- 支持多分类同时推荐，一顿完整的餐饮建议
- 点击"换一道"可单独更换某个分类的菜品

### 功能 2：忌口过滤系统
- 支持输入多个忌口关键词（用逗号分隔）
- 推荐结果自动排除包含忌口的菜品
- 忌口输入框支持快速重置

### 功能 3：数据持久化与管理
- 菜品数据存储在浏览器 localStorage，刷新不丢失
- 支持自定义分类（添加/删除分类）
- 支持菜品管理（添加/删除菜品）
- 推荐历史记录保存（最多20条）

---

## 五、数据模型

### 5.1 菜品数据 (localStorage)

| 字段名 | 类型 | 说明 | 示例 |
|-------|------|------|------|
| dishesData | Object | 菜品库，按分类存储（JSON对象键为分类名） | 见下方JSON |

```javascript
{
  "dishesData": {
    "主食": ["米饭", "面条", "馒头", "粥", "饺子", "包子", ...],
    "荤菜": ["宫保鸡丁", "红烧肉", "水煮鱼", "清蒸鲈鱼", ...],
    "素菜": ["麻婆豆腐", "蒜蓉西兰花", "清炒时蔬", ...],
    "小吃": ["寿司", "披萨", "汉堡", "沙拉", ...]
  }
}
```

**说明**：
- Key（分类名）：String 类型，如"主食"、"荤菜"
- Value（菜品列表）：Array<String> 类型，每个元素是菜品名称
- 支持动态添加/删除分类和菜品

---

### 5.2 历史记录数据 (localStorage)

| 字段名 | 类型 | 说明 | 示例 |
|-------|------|------|------|
| recommendHistory | Array | 推荐历史列表 | 见下方JSON |

| 数组元素字段 | 类型 | 说明 | 示例 |
|------------|------|------|------|
| date | String | 推荐日期时间 | "3-9 14:30" |
| dishes | String | 推荐菜品汇总（拼接文本格式："分类:菜品 分类:菜品"） | "主食:炒饭 荤菜:红烧肉" |

```javascript
{
  "recommendHistory": [
    {
      "date": "3-9 14:30",
      "dishes": "主食:炒饭 荤菜:红烧肉"
    },
    {
      "date": "3-9 12:15",
      "dishes": "主食:面条 素菜:番茄鸡蛋"
    }
  ]
}
```

**说明**：
- 最多保存 20 条记录（FIFO）
- 新推荐添加到列表头部
- 按时间倒序展示

---

### 5.3 运行时状态（非持久化）

| 字段名 | 类型 | 说明 | 默认值 |
|-------|------|------|-------|
| selectedCategories | Array<String> | 当前选中的分类列表 | ["主食", "荤菜"] |
| currentManageCategory | String \| Null | 菜品管理面板当前查看的分类 | null |

---

### 5.4 Supabase 数据库模型（升级目标）

> 以下为未来接入 Supabase 时的数据结构设计，与当前 localStorage 版本兼容但更结构化。

#### 5.4.1 分类表 (categories)

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | UUID | 主键 |
| name | TEXT | 分类名称 |
| color | TEXT | 分类颜色 |
| sort_order | INT | 排序 |
| created_at | TIMESTAMPTZ | 创建时间 |
| updated_at | TIMESTAMPTZ | 更新时间 |

#### 5.4.2 菜品表 (dishes)

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | UUID | 主键 |
| name | TEXT | 菜品名称 |
| category_id | UUID | 关联分类ID |
| is_active | BOOLEAN | 是否启用 |
| created_at | TIMESTAMPTZ | 创建时间 |
| updated_at | TIMESTAMPTZ | 更新时间 |

#### 5.4.3 推荐历史表 (recommend_history)

| 字段 | 类型 | 说明 |
|-----|------|------|
| id | UUID | 主键 |
| user_id | UUID | 用户ID |
| selected_categories | JSONB | 选中的分类数组 |
| recommended_dishes | JSONB | 推荐结果（键值对格式） |
| taboo_input | TEXT | 忌口输入 |
| recommend_date | TIMESTAMPTZ | 推荐时间 |

**与 localStorage 版本对比**：
| localStorage | Supabase | 说明 |
|-------------|----------|------|
| date (String) | recommend_date (TIMESTAMPTZ) | 时间戳格式更精确 |
| dishes (拼接字符串) | recommended_dishes (JSONB) | 结构化存储，便于处理 |
| - | selected_categories | 记录推荐时的分类选择 |
| - | taboo_input | 记录忌口输入 |

---

## 六、页面结构

```
┌─────────────────────────────────────┐
│           今天吃什么                 │
├─────────────────────────────────────┤
│  [主食] [荤菜] [素菜] [小吃]         │ ← 分类选择（可多选）
│  ┌─────────────────────────────┐   │
│  │         随机推荐             │   │
│  └─────────────────────────────┘   │
│  输入忌口：____________________     │
├─────────────────────────────────────┤
│  推荐结果：                         │
│  ┌─────────────────────────────┐   │
│  │ 主食:炒饭  [换一道]          │   │
│  │ 荤菜:红烧肉 [换一道]         │   │
│  │ 素菜:清炒时蔬 [换一道]       │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  推荐历史：                         │
│  ┌─────────────────────────────┐   │
│  │ 主食:米饭 荤菜:红烧肉 3-8 12:30│
│  │ 主食:面条 素菜:麻婆豆腐 3-8 18:15│
│  └─────────────────────────────┘   │
│  [清空]                            │
├─────────────────────────────────────┤
│  菜品管理：                         │
│  [主食] [荤菜] [素菜] [小吃]       │
│  + 新分类: ________ [添加分类]     │
│  ┌─────────────────────────────┐   │
│  │ 米饭                        │   │
│  │ 面条 [删除]                 │   │
│  └─────────────────────────────┘   │
│  分类:主食 ▼  新菜品: _____ [添加] │
└─────────────────────────────────────┘
```

---

## 七、成功标准

### 7.1 功能验收标准

| 功能点 | 验收条件 |
|--------|---------|
| 随机推荐 | 点击按钮后，每个选中分类随机展示一道菜品 |
| 忌口过滤 | 输入忌口后，推荐结果不包含忌口菜品 |
| 换一道 | 点击按钮后仅该分类重新随机，其他分类保持不变 |
| 历史记录 | 推荐成功后自动保存，刷新页面后数据保留 |
| 分类管理 | 可添加新分类、删除分类，分类变更后推荐正常 |
| 菜品管理 | 可添加/删除菜品，菜品库实时更新 |

### 7.2 用户体验标准

| 指标 | 目标值 |
|-----|-------|
| 页面加载时间 | < 2 秒 |
| 推荐响应时间 | < 100ms |
| 动画流畅度 | 60fps |
| 移动端适配 | 正常显示，无布局错乱 |

### 7.3 数据持久化标准

| 场景 | 预期结果 |
|-----|---------|
| 刷新页面 | 菜品库、历史记录保留 |
| 关闭浏览器重新打开 | 菜品库、历史记录保留 |
| 清除浏览器缓存 | 恢复默认菜品数据 |

---

## 八、非功能需求

- **兼容性**：支持 Chrome、Safari、Edge、Firefox 最新版本
- **响应式**：适配 320px ~ 1920px 屏幕宽度
- **无障碍**：基础键盘操作支持
- **性能**：单文件 HTML，无外部依赖，首屏零请求

---

## 九、版本历史

| 版本 | 日期 | 更新内容 |
|-----|------|---------|
| v1.0 | 2026-03-09 | 初始版本：随机推荐 + 忌口过滤 |
| v1.1 | 2026-03-09 | 多分类支持 + 换一道功能 |
| v1.2 | 2026-03-09 | UI 优化：分类颜色区分 |
| v1.3 | 2026-03-09 | 新增：历史记录 + 自定义分类 + 推荐动画 |

---

**文档版本**：v1.3
**最后更新**：2026-03-09
