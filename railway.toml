[railway]
  schema = "railway.v0.json"

[build]
  builder = "nixpacks"
  buildCommand = "npm install && npm run build"
  runCommand = "node server.js"

[deploy]
  numReplicas = 1
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[environment.variables]
  NODE_ENV = "production"
